# Example .env.local file (for local development only)
# This file is for reference only - use .env for actual configuration
# Copy this to .env and update with your values

# Ethereum RPC URL (public, free)
# You can use public RPC endpoints or your own node
ETHEREUM_RPC_URL=https://ethereum-rpc.publicnode.com

# MongoDB connection string (local example)
# For local development, use local MongoDB
MONGODB_URI=mongodb://localhost:27017/ethereum_indexer

# For production with MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<username>:<password>@<host>/<dbname>?retryWrites=true&w=majority

# Indexer Configuration
# Start block number - the indexer will start syncing from this block
# Set to 0 to start from genesis, or any specific block number
START_BLOCK=0

# Batch size - number of blocks to process per sync cycle
INDEXER_BATCH_SIZE=10

# Sync interval in milliseconds (for continuous sync service)
# 5000 = 5 seconds
SYNC_INTERVAL=5000

